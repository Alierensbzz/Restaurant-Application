@using RestaurantApplication.VarlikKatmani.Models
@model List<Kullanici>
@{
    ViewBag.Title = "Listele";
}
<div class="container mt-5">
    <div class="row">
        <div class="col">
            <h2 class="text-white">Kullanıcı Listesi</h2>
            <a href="@Url.Action("Ekle","Kullanici")" class="btn btn-success float-right">Kullanıcı Ekle</a>
        </div>
    </div>
    <hr class="bg-light" />
    <div class="row">
        <div class="col">
            <div class="table-responsive">
                <table class="table table-striped table-dark">
                    <thead>
                        <tr>
                            <th>Eposta</th>
                            <th>Ad</th>
                            <th>Soyad</th>
                            <th>Yetki</th>
                            <th>Aksiyonlar</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Eposta</td>
                                <td>@item.Ad</td>
                                <td>@item.Soyad</td>
                                <td>@item.Yetki</td>
                                <td>
                                    <a href="@Url.Action("Sil","Kullanici" ,new {eposta = item.Eposta})" class="btn btn-danger btn-sm mr-2">Sil</a>
                                    <a href="@Url.Action("Duzenle","Kullanici" ,new {eposta = item.Eposta})" class="btn btn-primary btn-sm">Güncelle</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        background: url('/Images/restaurant1.jpg') no-repeat center center fixed;
        background-size: cover;
        color: #fff;
    }

    .btn {
        border-radius: 20px;
    }

    .table {
        margin-bottom: 0;
    }

        .table th, .table td {
            vertical-align: middle;
            text-align: center;
        }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: rgba(255, 255, 255, 0.1);
    }

    .table-striped tbody tr:nth-of-type(even) {
        background-color: rgba(255, 255, 255, 0.05);
    }
</style>
